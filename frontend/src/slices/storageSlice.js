import {createSlice, } from "@reduxjs/toolkit";

export const storageSlice = createSlice({
    name: 'storage',
    initialState: '',
    reducers: {
        setCurrentGame: (state, action) => {
            
        },
        saveInventoryState: (state, action) => {

        },
        loadInventoryState: (state, action) => {

        }
    }

})